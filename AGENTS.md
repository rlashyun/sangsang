# Agents.md — 리트리버 (Retriever)

> 이 파일은 모든 에이전트 세션(Codex · Claude Code)에 로드된다. **여기 있는 규칙은 어떤 작업에서도 예외 없다.**
> 상세는 `docs/PRD.md`로 넘긴다 — 아래 8장 문서 지도 참조.
> **`docs/PRD.md`가 사양의 기준이다.** 코드와 어긋나면 개발자에게 즉시 질문한다 — 9.1의 6건이 그 목록이다.
> 참조는 PRD의 기존 ID만 쓴다 — `P0`/`P1`(우선순위) · `M0`~`M4`(마일스톤) · `Q1`~`Q6`(미결) · 절 번호(`4.5` 등).

---

## 1. 이 프로젝트가 하는 일

경찰청 습득물 데이터를 매일 모아, **잃어버린 지점 반경 안의 보관기관과 그 기관이 지금 보관 중인 습득물**을 지도에서 바로 탐색하게 해 주는 웹 서비스.

접근 방식은 **"물품명으로 찾기"가 아니다.** 잃어버린 장소로 후보를 수백 건까지 좁힌 뒤, 그 안에서는 대충 적어도 찾아지게 한다. **공간이 1차 필터, 검색이 2차 필터다.** (PRD 4.1)

**이 시스템이 실패하는 가장 흔한 방식은 "물건이 DB에 있는데 지도에 안 보이는 것"이다.**
보관기관 매칭에 실패하면 `storage_location_id`가 NULL이고, 그 습득물은 지도에도 검색에도 나타나지 않는다. 사용자는 데이터가 없다고 오인하고 떠난다. PRD 6의 리스크 1행이며 영향도 **높음**이다. 매칭을 건드리는 작업은 전부 이 사실을 기준으로 판단한다.

대상 사용자 — 최근 며칠 안에 대중교통·역사·상업시설 주변에서 물건을 잃어버렸고, **정확한 물품명은 몰라도 대략적인 장소는 기억하는** 사람. (PRD 1.2)

---

## 2. 절대 규칙 — 어기면 제품이 무너진다

| # | 금지 | 근거 |
| --- | --- | --- |
| 1 | 카테고리 선별에 **부분일치** 사용 — `prdtClNm`은 허용 목록과 **정확히 일치**할 때만 수집 | PRD 4.5 (오분류 방지) |
| 2 | **동기화가 실패한 출처**의 보존기간 데이터 삭제 | PRD 4.5 · 5 신뢰성 (보존 우선) |
| 3 | 습득물 **원문 변형 저장** — `item_name`·`description`은 API 원문 그대로, 정규화는 `normalized_search_text`에만 | PRD 3.3 |
| 4 | 카카오 **REST 키**·Supabase **secret 키**를 브라우저로 전달 (JS 키만 전달 가능) | PRD 5 보안 |
| 5 | 매칭 실패 건에 **추정 좌표를 채워** 지도에 노출 — 미매칭은 노출하지 않는 것이 원칙 | PRD 6 |
| 6 | `anon`/`authenticated` 권한으로 테이블 접근 — `service_role` 전용 | PRD 5 보안 |
| 7 | `/api/found-items` 응답 **캐시** | PRD 3.3 · 5 |
| 8 | 크론 엔드포인트를 `Authorization: Bearer $CRON_SECRET` 검증 **없이** 실행 | PRD 5 보안 |
| 9 | Preview 환경을 **운영 Supabase**에 연결 | PRD 5 보안 |
| 10 | 6개 카테고리 **밖의 습득물** 수집 · 분실물 **신고 등록/직접 매칭** 기능 추가 | PRD 2.2 비목표 1·3 |

**5번 부연** — 매칭 실패를 "일단 보이게" 만들고 싶은 충동이 반드시 생긴다. 좌표를 추정해 채우면 사용자는 **없는 곳으로 물건을 찾으러 간다.** 해결책은 별칭 테이블 등록이지 추정이 아니다. 매칭률은 `ingestion_runs.unmatched_count`로 관측한다.

---

## 3. 시스템의 축 — 공간 우선 탐색과 보관기관 매칭

### 탐색 흐름 (런타임)

```
장소·주소 검색 (카카오)  →  { x, y }
반경 필터 (Haversine)     →  반경 내 기관 목록  →  location_ids

GET /api/found-items?location_ids=...&q=...
  ① 후보 조회   location_ids 묶음으로 습득물 조회      ← 공간 1차 필터
  ② 랭킹        rank_found_items(candidates, q)       ← 검색 2차 필터
```

**검색은 언제나 `location_ids` 스코프 안에서만 일어난다.** 전국 단위 물품 검색 경로는 없다. 후보가 수백 건으로 줄어 있기 때문에 느슨한 임계값을 써도 오탐이 감당 가능한 범위에 머문다 — 이 전제가 깨지면 임계값도 다시 잡아야 한다. (PRD 4.1 차별점 1)

### 보관기관 매칭 (수집 시)

```
원문 보관장소명 (raw_storage_name)
  ① 별칭 테이블 정확 일치
  ② 정규화명 / 기관명 / 괄호 제거 기본명 정확 일치
  ③ 접미사 포함 관계

후보 1개   → matched     → storage_location_id 부여
후보 2개+  → ambiguous   → NULL
후보 0개   → unmatched   → NULL

NULL인 건은 지도·검색 어디에도 나타나지 않는다.
```

구현: `_resolve_location()` — `src/retriever_lost_found/ingestion/service.py`

---

## 4. 시스템 불변식 — 참이어야 하는 것

**모든 설계·구현 판단은 이 목록에 대해 검증한다.**

| 불변식 | 근거 |
| --- | --- |
| **멱등성** — 같은 구간을 다시 수집해도 결과가 같다. `(item_source_code, atc_id, found_sequence)` 기준 upsert | PRD 3.1-1 |
| **출처 독립** — `partner` 실패가 `police`의 수집·정리를 막지 않는다. 실패한 출처만 삭제를 건너뛴다 | PRD 5 신뢰성 |
| **중복 실행 차단** — 출처당 `running` 레코드는 하나. `ingestion_runs_one_running_per_source` 부분 유니크 인덱스 | PRD 4.5 |
| **증분 수집** — 출처별 마지막 등록일을 다시 포함해 재조회하고, 보존기간 시작일을 하한으로 둔다. 매일 전체 구간을 다시 받지 않는다 | PRD 4.5 |
| **감사 로그** — 모든 실행이 `ingestion_runs`에 `status`·`unmatched_count`와 함께 남는다. 로그가 없으면 지표 측정 자체가 불가능하다 | PRD 3.1-8 |
| **원문 보존** — API 원본은 `raw_payload`에 그대로 남는다 | PRD 3.3 |
| **키 격리** — 브라우저에 나가는 것은 카카오 JS 키뿐. REST 키·Supabase secret 키·`CRON_SECRET`은 서버 전용 | PRD 5 |
| **실패 응답은 한국어** — 400 `ValueError`(입력 오류) / 502 `RuntimeError`(외부 장애) / 503(Supabase 미설정) / 500(정적 파일 누락)으로 구분해 반환한다 | PRD 4.5 |

---

## 5. 용어

| 용어 | 뜻 |
| --- | --- |
| **연계기관** | 경찰청 유실물 시스템에 연계된 지하철·공항·터미널 등. 출처 코드 `partner_api` |
| **경찰관서** | 경찰서 및 지구대·파출소. 출처 코드 `police_api` |
| **matched / unmatched / ambiguous** | 원문 보관장소명을 좌표가 있는 기관 레코드에 연결한 결과 상태. `matched`만 지도에 보인다 |
| **6개 카테고리** | 가방 · 의류(모자) · 전자기기(무선이어폰) · 지갑 · 휴대폰 · 카드. 이 밖은 수집 단계에서 제외 |
| **보존기간** | 출처별로 그 기간 밖의 데이터를 삭제한다. 사양은 **6개월**, 현재 코드는 **10일/20일** — 9.1 D1 |
| **증분 동기화** | 크론이 도는 방식. 전날 등록분을 겹쳐 다시 확인한다 |
| **반경 스코프** | 선택 지점에서 Haversine 거리 이내 기관 묶음. 습득물 조회의 1차 필터 |
| **퍼지 매칭** | 물품명 1.0 / 원분류 0.9 / 색상 0.82 / 설명·정규화 텍스트 0.72 가중 유사도. 편집거리 + 자카드 + 접두 보너스 결합 |

**ID 체계** — `P0`/`P1`(우선순위, PRD 4.2) · `M0`~`M4`(마일스톤, PRD 7.1) · `Q1`~`Q6`(미결, PRD 8) · 절 번호

---

## 6. 작업 규칙

| 규칙 | 이유 |
| --- | --- |
| **구현 근거를 PRD 절 번호·ID로 주석에 남긴다** — `# PRD 4.5 — 부분일치 금지` | 나중에 왜 그렇게 짰는지 복원 가능해야 한다 |
| **PRD가 기준이다. 코드가 어긋나면 코드가 틀린 것이다** | 실제로 6건 어긋나 있다(9.1). PRD 값을 목표로 맞춰 간다 |
| **단, PRD 값이 이미 구현되어 있다고 가정하지 않는다.** 만지기 전에 코드의 현재 동작을 확인한다 | 9.1의 6건이 전부 그 함정이다. PRD는 반경 1·3·5km를 "완료 ✅"로 적어 두었지만 코드는 1km 고정이다 |
| **PRD에 없는 것은 지어내지 말고 질문하고 멈춘다** | 미결 항목이 실제로 많다. 빈칸을 그럴듯하게 채우면 잘못된 결정이 코드에 굳는다 |
| **`main`에 직접 커밋하지 않는다. 브랜치 → PR** | CODEOWNERS가 모든 변경에 `@rlashyun` 승인을 요구한다 |
| **담당 파트 분할은 아직 없다.** 같은 파일을 동시에 건드릴 것 같으면 PR을 먼저 올려 범위를 드러낸다 | 4명이 파일 소유 없이 공유한다. 충돌은 리뷰에서 조율한다 |
| **환경 변수를 추가하면 `.env.example`·README·CI `env`를 같이 갱신한다** | 셋 중 하나만 빠져도 다른 팀원 로컬과 CI가 깨진다 |
| **의존성은 `uv add`로 추가하고 `uv.lock`을 커밋한다** | `uv sync --locked`로 전원이 같은 환경을 쓴다 |
| **한 세션에 작업 단위 하나** | 컨텍스트가 길어지면 앞의 제약(감사 로그 적재, 키 격리)을 뒤쪽에서 빠뜨린다 |

### 세션 완료 조건

① PRD의 해당 항목 충족 ② `uv run pytest` 통과 ③ 근거 주석 존재 ④ 환경 변수·문서 갱신 반영

---

## 7. 테스트 규칙

| 규칙 | 이유 |
| --- | --- |
| **외부 API를 실제로 호출하는 테스트를 만들지 않는다** | CI는 더미 키로 돈다(`.github/workflows/ci.yml`). 스텁·페이크 클라이언트를 쓴다 |
| **negative는 반드시 positive와 쌍으로** | "반경 밖 기관이 0건"만 확인하면 쿼리가 **항상** 빈 결과를 반환해도 통과한다. 반경 안에서는 보이는지 함께 확인한다 |
| **"0건"으로 단정할 곳은 "0건"으로 쓴다.** "대부분" "거의"로 쓰지 않는다 | 미매칭 건 노출, 카테고리 밖 수집은 1건도 통과가 아니다 |
| **테스트가 실패하면 테스트를 고치지 않는다. 구현을 고친다** | 가장 흔한 실패 모드. 특히 임계값 assertion을 느슨하게 바꾸는 일 |
| **보안·수집 규칙 테스트를 skip·주석 처리하지 않는다** | 2장 절대 규칙이 실제로 지켜지는지 확인하는 유일한 수단이다 |
| **테스트용 우회 경로(인증 무시 플래그 등)를 만들지 않는다** | 그 경로가 곧 우회 표면이다 |
| 테스트는 영역별 디렉토리에 둔다 — `tests/{database,deployment,ingestion,web}/` | 현재 구조를 따른다 |

실행: `uv run pytest` · CI는 `main` 대상 PR과 push에서 자동 실행

---

## 8. 문서 지도

| 문서 | 언제 읽나 |
| --- | --- |
| **`docs/PRD.md`** | 착수 전 1회, 그리고 작업 항목마다 — 범위·우선순위·예외 케이스 |
| **`docs/open-questions.md`** | 9장 항목을 맡았을 때 — D1~D6 결정 근거, 열린 질문(`Q1`~`Q6`), PRD의 "결정 필요" 항목 |
| **`README.md`** | 환경 구성·실행 명령·환경 변수 |
| `.env.example` | 필요한 환경 변수 목록 |
| `supabase/migrations/` | 스키마의 사실상 원본 |
| `vercel.json` | 크론 스케줄·함수 제한 |

### 규칙 파일이 두 개인 이유

**이 파일(`AGENTS.md`)이 유일한 원본이다.** 루트 `CLAUDE.md`는 `@AGENTS.md` **import 한 줄**이며 내용이 없다.

Codex는 `AGENTS.md`를 읽고, Claude Code는 `CLAUDE.md`가 있으면 **그쪽만** 읽는다 — `CLAUDE.md`가 존재하는 순간 기본 설정에서 `AGENTS.md`는 무시된다. import 한 줄이 그 둘을 다시 잇는다.

| 하면 안 되는 것 | 왜 |
| --- | --- |
| `CLAUDE.md`에 규칙 내용을 쓴다 | 두 파일이 갈라진다. Claude 세션만 다른 규칙을 보게 된다 |
| `CLAUDE.md`를 "중복"으로 보고 지운다 | Claude Code v2.1.277 미만, Bedrock, 텔레메트리 비활성 세션에서 `AGENTS.md`가 로드되지 않는다 |
| `@AGENTS.md`를 "규칙은 AGENTS.md 참조" 같은 **산문**으로 바꾼다 | import가 아니면 자동 로드가 아니다. 모델이 읽어줄 수도, 안 읽을 수도 있다 |

Claude 전용 지침이 필요하면 `CLAUDE.md`의 import **아래에** 덧붙인다. 공통 규칙은 전부 이 파일에 쓴다.
로드 확인 — Claude Code에서 `/context`의 **Memory files**에 `CLAUDE.md`가 보이면 정상이다.

**PRD 절 참조 단축키** — 2.2(비목표) · 2.5(가드레일) · 3.3(가정·제약) · 4.2(기능 우선순위) · 4.3(사용자 플로우) · **4.5(핵심 로직·예외 케이스)** · 5(비기능) · 6(리스크) · 7.1(마일스톤) · 8(미결)

**코드 지도**

```
api/index.py                 Vercel 진입점 (web/app.py의 FastAPI app 재사용)
src/retriever_lost_found/
├─ config.py                 환경 변수 로딩
├─ ingestion/                카테고리 선별 · 수집 · 저장 · 동기화 · 기관 매칭
├─ integrations/             Kakao · Supabase · 경찰청 API 연동
├─ search/fuzzy.py           가중 퍼지 유사도와 랭킹
└─ web/                      FastAPI 라우트 · 크론 · 정적 UI
supabase/migrations/         재현 가능한 DB 스키마
tests/                       기능 영역별 테스트
```

**엔드포인트** — `/` · `/api/geocode` · `/api/institutions`(CDN 5분 캐시) · `/api/found-items`(무캐시) · `/api/places` · `/api/cron/{partner,police}-sync`(Bearer 인증)

---

## 9. PRD 미준수와 예정 작업

**PRD에 없는 것은 지어내지 말고 질문하고 멈춘다.**
열린 질문(`Q1`~`Q6`)·PRD의 "결정 필요" 항목·결정 근거는 **`docs/open-questions.md`**로 넘겼다. 여기에는 매 세션 필요한 두 가지만 남긴다 — **결정 요약표**와 **아직 반영되지 않은 코드 변경**.

### 9.1 PRD 미준수 6건 — 결정 완료

"어느 쪽이 맞나"의 문제가 아니다. **PRD 값이 사양이고 코드가 아직 못 따라온 것**이다.
다만 **현재 코드 동작을 모르고 작업하면 그대로 함정에 빠진다.** 두 값을 모두 적어 둔 이유다.

| No | 항목 | 사양 (PRD) | 현재 코드 | **결정 (2026-09-21)** |
| --- | --- | --- | --- | --- |
| **D1** | 보존기간 | 연계기관·경찰관서 **6개월** (3.1-2, 4.1) | **10일 / 20일** | **상수만 180일로 바꾸고 자연 누적.** 백필하지 않는다 — 아래 |
| **D2** | 유사도 % 노출 | PRD 자기모순이었음 | `found-items.js`가 `유사도 87%`로 **표시 중** | **숨긴다.** PRD 3.1-6·4.5를 살리고 **4.1 차별점 2를 수정**. 코드에서 % 표시 제거 |
| **D3** | 검색어 길이 상한 | **30자** (3.3) | `app.py` **100자**, `index.html` maxlength 200(장소)/100(물품) | **30자로 내린다** (PRD 3.3대로) — 아래 |
| **D4** | 반경 1·3·5km 선택 | **P0.** PRD는 "완료 ✅"로 적혀 있으나 **미구현** (3.1-3, 4.2) | `SEARCH_RADIUS_METERS = 1000` **고정, 선택 UI 없음** | **PRD의 ✅ 표기를 먼저 정정.** 구현 시점은 D6 일정을 보고 별도로 정한다 |
| **D5** | 매칭 실패 대응 | **P0.** "스크립트 존재"로 적혀 있음 (4.3-D) | `tools/` 전체가 커밋 `09cca07`에서 **삭제됨**. README 구조도에는 남아 있음 | **관측부터.** 현재 매칭률을 먼저 측정하고, 그 수치를 보고 도구를 정한다 |
| **D6** | 프런트엔드 스택 | **React + TypeScript + Vite + React Router** (10 부록) | 정적 HTML/CSS/JS (`web/static/`) | **전환한다.** 전환 전까지 UI 작업은 `web/static/`에 |

⚠️ **D1 — 보존기간은 두 군데에 있다.** `SOURCE_RETENTION_DAYS`(수집 창)와 DB `item_sources.retention_days`(삭제). **한쪽만 바꾸면 매일 6개월치를 가져와 매일 20일 밖을 지운다.**

각 결정의 근거는 **`docs/open-questions.md` 1장**에 있다.

---

### 9.2 필요한 코드 변경 — ⚠️ **아직 하나도 반영되지 않았다**

**결정만 된 상태이고 코드는 그대로다.** 아래를 이미 적용된 것으로 가정하고 작업하면 어긋난다.
D1·D2·D3은 성격이 달라 **각각 별도 PR**로 진행한다.

| No | 파일 | 변경 |
| --- | --- | --- |
| **D1** | `ingestion/collector.py` — `SOURCE_RETENTION_DAYS` | `{"partner": 10, "police": 20}` → **`{"partner": 180, "police": 180}`** |
| **D1** | 새 마이그레이션 | `update public.item_sources set retention_days = 180;` — **위 변경과 반드시 같은 PR에서.** 따로 나가면 매일 가져와서 매일 지운다 |
| **D2** | `web/static/found-items.js` | 결과 카드의 `match-score` span 생성·삽입 제거 |
| **D2** | `web/static/styles.css` | `.match-score` 규칙 제거 |
| **D3** | `web/app.py` — `found_items()` | 상한 `100` → **`30`**, 오류 메시지의 자릿수도 함께 |
| **D3** | `web/static/index.html` — `#found-item-input` | `maxlength="100"` → **`"30"`** |

**건드리지 말 것**

- `tests/web/test_kakao_map.py`의 `match_score` 단언 — D2는 **화면 노출만** 막는다. 랭킹 점수 자체는 내부 평가에 계속 쓴다(PRD 4.5). 이 테스트는 그대로 둔다
- `index.html`의 `maxlength="200"` — **장소 검색칸**이다. PRD 3.3의 30자는 **습득물 검색어**에만 해당한다

**같이 해야 할 것**

- **D3에는 현재 상한 테스트가 없다.** 30자로 내릴 때 *30자 통과 / 31자 400* 쌍으로 추가한다 (7장 — negative는 positive와 쌍으로)
- **D4를 구현할 때** `tests/web/test_map_radius.js`의 1000m 단언도 함께 손본다

**코드 변경이 아닌 것**

- **D5 관측** — 코드를 고칠 필요가 없다. `ingestion_runs`의 `unmatched_count / selected_count`를 조회하면 된다. 수치를 본 뒤에 도구를 정한다
- **D4 구현 시점** — 미정. D6(React 전환) 일정을 보고 정한다

---

## 10. 커밋과 PR

```
{type}: {요약} — {PRD ID}

예: feat: 반경 3·5km 선택 UI — P0, PRD 4.2
    fix: 2자 이하 질의 임계값 0.7 적용 — PRD 4.5
    test: 미매칭 건이 지도 응답에 포함되지 않음 — PRD 6
    docs: 보존기간 서술을 코드와 일치시킴 — D1
```

`type` — `feat` · `fix` · `test` · `docs` · `ci` · `chore` · `refactor`
담당 파트 분할이 아직 없으므로 스코프는 비워 둔다. 분할이 정해지면 `feat(ingestion):` 형태로 바꾼다.

**브랜치 → PR이 유일한 경로다.** CODEOWNERS(`* @rlashyun`)가 모든 변경에 소유자 승인을 요구하며, CI(`pytest`)가 `main` 대상 PR에서 돈다.

```
git switch -c {type}-{짧은-설명}
# 작업
git push -u origin {브랜치}
gh pr create
```

---

## 11. 일정

PRD 7.1의 마일스톤은 **날짜가 고정되어 있지 않다("유동적으로 조정")**. 아래는 순서와 완료 조건이며, 날짜는 팀이 별도로 정한다.

| 마일스톤 | 완료 조건 |
| --- | --- |
| **M0** MVP 동작 | 배포본에서 장소 검색 → 물품 목록까지 동작 |
| **M1** 데이터 신뢰성 | `matched` 비율 99% 이상 · 크론 7일 연속 성공 **(확인 필요)** |
| **M2** 탐색 품질 검증 | p95 1.5초 이하 · TOP-5 포함률 95% 이상 · 거절 성능 95% 이상 · nDCG@5 · 검색 1,000회당 비용 |
| **M3** 수령 연결 완결 | 기관 주소·전화번호 안내 후 사용자 테스트 |
| **M4** 이미지 검색 | M2와 동일 기준 + 임베딩 파이프라인 |

---

## 12. 변경 이력

| 날짜 | 변경 내용 |
| --- | --- |
| 2026-09-21 | `docs/PRD.md`와 MVP 코드베이스를 대조해 초안 작성 |
| 2026-09-21 | **PRD를 사양의 기준으로 확정.** 9.1을 "어느 쪽이 맞나"에서 "PRD 미준수 목록"으로 재작성. D2는 PRD 자체가 모순이라 열린 질문으로 분리 |
| 2026-09-21 | 에이전트 자동 로드를 위해 `docs/Agents.md` → 저장소 루트 `AGENTS.md`로 이동 |
| 2026-09-21 | Claude Code용으로 루트 `CLAUDE.md`(`@AGENTS.md` import 한 줄) 추가. 이 파일이 유일한 원본 — 8장 참조 |
| 2026-09-21 | D1~D6 결정 기록. D3는 30자로 확정. 필요한 코드 변경을 9.2에 목록화 — **코드는 아직 미반영** |
| 2026-09-21 | 9장의 결정 근거·열린 질문을 `docs/open-questions.md`로 분리 (349줄 → 295줄). 결정 요약표와 코드 변경 목록만 남김 |
